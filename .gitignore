# Python
__pycache__/
*.py[cod]
*.pyo
.env
.venv/
env/
venv/
*.egg-info/
dist/
build/
.pytest_cache/

# Jupyter
.ipynb_checkpoints/

# LaTeX — artefactos de compilación (no rastrear en git)
*.aux
*.log
*.out
*.toc
*.synctex.gz
*.fls
*.fdb_latexmk
*.bbl
*.blg
manuscript/*.pdf
# PDFs sueltos compilados en la raíz (las fuentes viven en manuscript/)
/*.pdf

# Boltzmann code forks — externos y voluminosos, no se versionan aquí
# (clonar por separado: CLASS / EFTCAMB upstream)
eftcamb_ssee/

# Obsidian vault — notas personales, no parte del repo científico
.obsidian/

# Data (large files — track separately or via DVC)
data/raw/*.fits
data/raw/*.hdf5
data/raw/*.h5

# Results (generated — reproducible from scripts)
results/logs/*.txt
results/logs/*.npz
*.npz

# Virtual environments
venv_*/

# Sandbox experimental separado (tiene su propio .git, no incluir aquí)
sandbox_unificado/

# MCMC checkpoint outputs en src/ (artefactos de runs, no source)
src/results/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
