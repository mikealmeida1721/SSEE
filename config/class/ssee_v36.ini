# ============================================================
# CLASS input file: SSEE-V3.6 Background Configuration
# Structural Self-Energy Expansion — Zero free parameters
# w0 = -T_r/M_v = -(3phi+pi)/(2*(phi+pi)) ~ -0.8399
# wa = -(2phi+pi)/(2*(phi+pi))             ~ -0.6699
# Omega_phi = (3phi+pi)^2 / (4*(phi+pi)^2) ~ 0.8399
# ============================================================

# --- Output ---
output = tCl,pCl,lCl,mPk
lensing = yes
l_max_scalars = 2500
P_k_max_h/Mpc = 1.0

# --- SSEE Dark Energy (CPL fluid, algebraically fixed) ---
# These are the SSEE algebraic predictions, NOT fitted values
# NOTE: Omega_Lambda is NOT set — CLASS computes the residual automatically
Omega_fld = 0.8399         # = (3phi+pi)^2 / (4*(phi+pi)^2)
w0_fld = -0.8399           # = -T_r/M_v = -(3phi+pi)/(2*(phi+pi))
wa_fld = -0.6699           # = -(2phi+pi)/(2*(phi+pi))
cs2_fld = 1.0              # sound speed squared (standard)

# --- Matter sector (SSEE two-sector: use MIRA-mapped Omega_m) ---
# Omega_m,CMB = Omega_m,dyn * MIRA = 0.160 * 1.9989 = 0.3199
Omega_b = 0.04897          # Planck 2018
Omega_cdm = 0.2710         # 0.3199 - 0.04897 ≈ 0.2710 (MIRA-mapped)
N_eff = 3.046
T_cmb = 2.7255

# --- Primordial spectrum (SSEE algebraic prediction) ---
# n_s = 1 - phi^{-7} = 0.96556
n_s = 0.96556
A_s = 2.1e-9               # Standard normalization (not algebraically fixed)
k_pivot = 0.05

# --- Hubble (SSEE prediction: H0 = 3*(phi+pi)^2 = 67.962) ---
h = 0.67962

# --- Output files ---
root = output/ssee_v36_
overwrite_root = yes
